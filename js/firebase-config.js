// Firebase configuration for the shared-library sync feature.
//
// Sync is OFF until you replace `null` below with your Firebase project's
// web config object — see SETUP-SYNC.md in the repo root for the 5-minute
// walkthrough. The config looks like:
//
// export const firebaseConfig = {
//   apiKey: "AIza...",
//   authDomain: "your-project.firebaseapp.com",
//   projectId: "your-project",
//   storageBucket: "your-project.appspot.com",
//   messagingSenderId: "1234567890",
//   appId: "1:1234567890:web:abc123",
// };
//
// (A Firebase *web* config is safe to publish — access control comes from
// your Firestore security rules, not from keeping these values secret.)

export const firebaseConfig = null;
