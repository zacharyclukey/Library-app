# No trailing slash: this is sometimes a symlink to a shared install rather
# than a real folder, and "node_modules/" only matches a directory.
node_modules
# Screenshots the test suites write as they run
tests/*.png
*.log
.DS_Store
